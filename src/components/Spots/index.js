import React, { useEffect } from 'react';
import View from './view';

const Spots = props => {
  useEffect(() => {
    async function getSpots() {
      
    }
  });

  return (
    <View />
  );
};

export default Spots;
