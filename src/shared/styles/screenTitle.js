import { StyleSheet } from 'react-native';
import colors from './colors';

export default StyleSheet.create({
  text: {
    color: colors.main,
    fontWeight: '700',
    fontSize: 16,
    textAlign: 'center',
  },
});
